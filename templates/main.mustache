{{! <h3><a data-toggle="tooltip" data-placement= "top" title="'.$course->fullname.'" href="'.$courseurl.'">'.$trimtitle.'</a></h3> }}
{{! <span data-toggle="tooltip" data-placement="top" title="tooltip testada">
testando tooltip
</span> }}
<div id="block-class_material-{{uniqid}}" data-course-module-id="{{courseModuleId}}">
    <p>Está atividade não possui material.</p>

{{#canEdit}}
    <div class="d-flex align-items-center justify-content-center">
        <button class="btn btn-primary" id="add_material">
            Adicionar
        </button>
    </div>
{{/canEdit}}
</div>
{{#js}}
require(
[
    'jquery',
    'block_class_material/main',
],
function(
    $,
    Main
) {
    var root = $('#block-class_material-{{uniqid}}');
    Main.init(root);
});
{{/js}}
